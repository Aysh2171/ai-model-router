# Capability Matcher Dependencies
# The Capability Matcher prototype uses Python Standard Library only (uuid, dataclasses, typing, pathlib).
# It integrates with model_registry locally without external package dependencies.
