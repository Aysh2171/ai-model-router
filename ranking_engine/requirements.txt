# Ranking Engine Dependencies
# The Ranking Engine prototype uses Python Standard Library only (dataclasses, json, pathlib, typing, unittest).
# It integrates locally with rule_engine, capability_matcher, and model_registry without external package dependencies.
