# Rule Engine Dependencies
# The Rule Engine prototype uses Python Standard Library only (uuid, dataclasses, typing, pathlib, json, abc).
# It integrates with capability_matcher and model_registry locally without external package dependencies.
